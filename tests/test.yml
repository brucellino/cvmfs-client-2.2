---
- hosts: localhost
  remote_user: root
  roles:
    - {name: cvmfs-client-2.2, become: true}

- hosts: localhost
  remote_user: cvmfs
  vars:
    CODE_RADE_VERSION: "{{ lookup('file', '/cvmfs/fastrepo.sagrid.ac.za/version') | string }}"
  tasks:
  - name: CODE-RADE version
    debug:
      var: "{{ CODE_RADE_VERSION }}"
